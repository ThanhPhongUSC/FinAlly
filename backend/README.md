# FinAlly Backend

FastAPI backend for FinAlly, managed as a `uv` project.

## Setup

```bash
uv sync
```

## Run tests

```bash
uv run pytest
```

## Run the dev server

```bash
uv run uvicorn app.main:app --reload --port 8000
```
